export enum ExpiresInMinutes {
  Verification = 30,
  ResetPassword = 15
}

export enum Mimetype {
  Jpeg = 'image/jpeg',
  Png = 'image/png'
}

export enum ImageSizeInMb {
  Ten = 10
}

export enum MediaRefType {
  User = 'User',
  Video = 'Video'
}
